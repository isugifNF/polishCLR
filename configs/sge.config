process {
    executor = 'sge'
    clusterOptions = params.clusterOptions ? "$params.clusterOptions" : ' '
    // queue = 'long'  // <= listed here as potential nextflow sge directives, clusterOptions is probably safer
    // memory = '10GB' 
}

executor {
  queueSize = "$params.queueSize"
  submitRateLimit = '10 sec'
}
